/**
 * Drizzle schema (SQLite / Cloudflare D1).
 *
 * SCAFFOLD PLACEHOLDER — real tables land in epic 03-identity-auth/01:
 *   - better-auth core: user (+ isAnonymous, displayName, avatarId),
 *     session, account, verification
 *   - favorite_rooms
 *   - inert: matches, match_players  (schema only, no writes in Phase 1)
 *
 * Do not run `db:generate` until that story adds the tables.
 */
export {};
