# 07 · CI

**Status:** done

## Done

`.github/workflows/ci.yml` — on `push` to `main` and all PRs:

1. `actions/checkout`
2. `pnpm/action-setup@v4`
3. `actions/setup-node@v4` with `node-version-file: .node-version`, pnpm cache
4. `pnpm install --frozen-lockfile`
5. `pnpm format:check`
6. `pnpm -r typecheck`
7. `pnpm lint`
8. `pnpm -r test`
9. `pnpm --filter @wordle-clash/web build`

No deploy automation in Phase 1.

## Follow-ups

- Add the Worker test job (`@cloudflare/vitest-pool-workers`) once epic 02 lands
  RoomServer tests — it needs `wrangler` available in CI (already a dev dep).
- Consider a preview-deploy-on-PR job later.
